const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: 'avatar.vercel.sh',
      },
      {
        hostname: 'dd.dexscreener.com',
      },
    ],
  },
};

module.exports = nextConfig;
